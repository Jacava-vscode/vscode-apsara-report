name: "DISABLED: Update Backend Repo (previously automated)"

on:
  workflow_dispatch: {}

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow was disabled in the repository. The automated update-backend-repo flow has been removed." 
