name: "DISABLED: Push Backend Mirror"

on:
  workflow_dispatch: {}

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow was disabled in the repository. The automated push-backend-mirror flow has been removed." 
