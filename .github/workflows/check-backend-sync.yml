name: "DISABLED: Check Backend Mirror"

on:
  workflow_dispatch: {}

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow was disabled in the repository. The backend mirror check flow has been removed." 
