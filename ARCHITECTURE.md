# 🏗️ System Architecture

## Overview Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                         CLIENT BROWSER                       │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Home Page  │  │  Dashboard   │  │   Form Page  │     │
│  │  index.html  │  │dashboard.html│  │  form.html   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              List View (list.html)                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           JavaScript Modules                         │  │
│  │  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐   │  │
│  │  │ api.js │  │form.js │  │list.js │  │dash.js │   │  │
│  │  └────────┘  └────────┘  └────────┘  └────────┘   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              CSS Styling (styles.css)                 │  │
│  └──────────────────────────────────────────────────────┘  │
└──────────────────────────┬───────────────────────────────────┘
                          │ HTTP/REST API
                          │ (JSON)
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    EXPRESS SERVER (Node.js)                  │
│                      Port: 5000                              │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              server/index.js                         │  │
│  │         (Main application entry point)               │  │
│  └──────────────────────────────────────────────────────┘  │
│                          │                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API Routes                               │  │
│  │         server/routes/equipment.js                    │  │
│  │                                                        │  │
│  │  • GET    /api/equipment           (Get all)         │  │
│  │  • GET    /api/equipment/:id       (Get one)         │  │
│  │  • POST   /api/equipment           (Create)          │  │
│  │  • PUT    /api/equipment/:id       (Update)          │  │
│  │  • DELETE /api/equipment/:id       (Delete)          │  │
│  │  • GET    /api/equipment/stats/summary (Stats)       │  │
│  └──────────────────────────────────────────────────────┘  │
│                          │                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │          Google Drive Service                         │  │
│  │      server/services/driveService.js                  │  │
│  │                                                        │  │
│  │  • authorize()         - OAuth authentication        │  │
│  │  • readData()          - Read from Drive             │  │
│  │  • writeData()         - Write to Drive              │  │
│  │  • findOrCreateFile()  - File management             │  │
│  └──────────────────────────────────────────────────────┘  │
└──────────────────────────┬───────────────────────────────────┘
                          │ Google Drive API
                          │ (OAuth 2.0)
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                      GOOGLE DRIVE                            │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         Your Equipment Data Folder                    │  │
│  │                                                        │  │
│  │  📄 equipment_data.json                               │  │
│  │     [                                                 │  │
│  │       { id: "...", type: "computer", ... },          │  │
│  │       { id: "...", type: "printer", ... },           │  │
│  │       ...                                             │  │
│  │     ]                                                 │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. Adding Equipment

```
User (Browser)
    │
    ├─> Fills form (form.html)
    │
    ├─> Submits form
    │
    ├─> form.js validates data
    │
    ├─> api.js sends POST request
    │
    ▼
Express Server
    │
    ├─> equipment.js receives request
    │
    ├─> Generates UUID
    │
    ├─> Adds timestamps
    │
    ├─> driveService.readData()
    │
    ├─> Appends new item
    │
    ├─> driveService.writeData()
    │
    ▼
Google Drive
    │
    ├─> Updates equipment_data.json
    │
    ▼
Response flows back
    │
    ├─> Success message
    │
    ├─> Redirects to list.html
    │
    ▼
User sees updated list
```

### 2. Viewing Dashboard

```
User (Browser)
    │
    ├─> Opens dashboard.html
    │
    ├─> dashboard.js loads
    │
    ├─> api.js requests stats
    │
    ▼
Express Server
    │
    ├─> equipment.js stats endpoint
    │
    ├─> driveService.readData()
    │
    ├─> Calculates statistics
    │
    ├─> Returns aggregated data
    │
    ▼
Browser
    │
    ├─> dashboard.js receives data
    │
    ├─> Updates stat cards
    │
    ├─> Chart.js renders charts
    │
    ▼
User sees visualizations
```

### 3. Searching/Filtering

```
User (Browser)
    │
    ├─> Types in search box (list.html)
    │
    ├─> list.js filters locally
    │
    ├─> Re-renders table
    │
    ▼
Updated view (no server call needed)
```

## Component Relationships

```
┌─────────────────────────────────────────────────────┐
│                   index.html                        │
│  • Homepage                                         │
│  • Quick stats                                      │
│  • Recent items                                     │
│  • Navigation to other pages                        │
└─────────────────────────────────────────────────────┘
                    │
      ┌─────────────┼─────────────┬─────────────┐
      │             │             │             │
      ▼             ▼             ▼             ▼
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│Dashboard │  │   Form   │  │   List   │  │   API    │
│          │  │          │  │          │  │          │
│ Charts   │  │ Add/Edit │  │ Browse   │  │ Backend  │
│ Stats    │  │ Validate │  │ Search   │  │ CRUD     │
└──────────┘  └──────────┘  └──────────┘  └──────────┘
      │             │             │             │
      └─────────────┴─────────────┴─────────────┘
                    │
                    ▼
            ┌───────────────┐
            │  Google Drive │
            │   Storage     │
            └───────────────┘
```

## File Dependencies

```
server/index.js
    ├── requires: express, cors, body-parser, dotenv
    ├── imports: server/routes/equipment.js
    └── serves: client/* files

server/routes/equipment.js
    ├── requires: express, uuid
    └── imports: server/services/driveService.js

server/services/driveService.js
    ├── requires: googleapis, fs
    └── reads: credentials.json, token.json

client/index.html
    ├── includes: css/styles.css
    └── scripts: js/api.js

client/dashboard.html
    ├── includes: css/styles.css
    ├── scripts: js/api.js, js/dashboard.js
    └── external: Chart.js CDN

client/form.html
    ├── includes: css/styles.css
    └── scripts: js/api.js, js/form.js

client/list.html
    ├── includes: css/styles.css
    └── scripts: js/api.js, js/list.js
```

## Authentication Flow

```
┌─────────────────────────────────────────────────────┐
│  1. First Time Setup                                │
│                                                     │
│  User runs: npm run auth                            │
│      │                                              │
│      ├─> server/auth-helper.js                      │
│      │                                              │
│      ├─> Reads credentials.json                     │
│      │                                              │
│      ├─> Generates OAuth URL                        │
│      │                                              │
│      ├─> User opens URL in browser                  │
│      │                                              │
│      ├─> User authorizes app                        │
│      │                                              │
│      ├─> Google redirects with code                 │
│      │                                              │
│      ├─> User pastes code back                      │
│      │                                              │
│      ├─> Exchanges code for tokens                  │
│      │                                              │
│      └─> Saves to token.json                        │
│                                                     │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  2. Subsequent Requests                             │
│                                                     │
│  Server starts                                      │
│      │                                              │
│      ├─> driveService.authorize()                   │
│      │                                              │
│      ├─> Reads token.json                           │
│      │                                              │
│      ├─> Creates OAuth client                       │
│      │                                              │
│      ├─> Makes API requests with token              │
│      │                                              │
│      └─> Token auto-refreshes if expired            │
│                                                     │
└─────────────────────────────────────────────────────┘
```

## Technology Stack Layers

```
┌─────────────────────────────────────────────────────┐
│  Presentation Layer                                 │
│  • HTML5 (Structure)                                │
│  • CSS3 (Styling)                                   │
│  • JavaScript ES6+ (Logic)                          │
│  • Chart.js (Visualization)                         │
└─────────────────────────────────────────────────────┘
                    ▲
                    │ HTTP/JSON
                    ▼
┌─────────────────────────────────────────────────────┐
│  Application Layer                                  │
│  • Express.js (Web Framework)                       │
│  • Node.js (Runtime)                                │
│  • REST API (Interface)                             │
│  • CORS (Security)                                  │
└─────────────────────────────────────────────────────┘
                    ▲
                    │ Google Drive API
                    ▼
┌─────────────────────────────────────────────────────┐
│  Data Layer                                         │
│  • Google Drive (Storage)                           │
│  • JSON (Format)                                    │
│  • OAuth 2.0 (Authentication)                       │
└─────────────────────────────────────────────────────┘
```

## Request/Response Cycle

```
┌──────────┐
│  CLIENT  │
└────┬─────┘
     │
     │ 1. HTTP Request (e.g., POST /api/equipment)
     │    Headers: Content-Type: application/json
     │    Body: { type: "computer", brand: "Dell", ... }
     │
     ▼
┌──────────────────────┐
│   EXPRESS ROUTER     │
│  equipment.js        │
└────┬─────────────────┘
     │
     │ 2. Route handler processes request
     │    - Validates data
     │    - Generates UUID
     │    - Adds timestamps
     │
     ▼
┌──────────────────────┐
│   DRIVE SERVICE      │
│  driveService.js     │
└────┬─────────────────┘
     │
     │ 3. Google Drive API call
     │    - Authenticate with OAuth
     │    - Read current data
     │    - Append new item
     │    - Write back to Drive
     │
     ▼
┌──────────────────────┐
│   GOOGLE DRIVE       │
│  equipment_data.json │
└────┬─────────────────┘
     │
     │ 4. Response flows back
     │
     ▼
┌──────────────────────┐
│   EXPRESS ROUTER     │
└────┬─────────────────┘
     │
     │ 5. Send JSON response
     │    Status: 201 Created
     │    Body: { success: true, data: {...} }
     │
     ▼
┌──────────┐
│  CLIENT  │
└──────────┘
     │
     │ 6. JavaScript processes response
     │    - Shows success message
     │    - Updates UI
     │    - Redirects if needed
     │
     ▼
   [DONE]
```

## Deployment Architecture

```
Development:
  localhost:5000 → Express Server → Google Drive

Production Options:
  1. Heroku/Railway → Express Server → Google Drive
  2. VPS (Linux) → Express Server → Google Drive
  3. Docker Container → Express Server → Google Drive

Note: Frontend can be served by Express or separately via CDN
```

This architecture provides:
- ✅ Separation of concerns
- ✅ Scalability
- ✅ Maintainability
- ✅ Security through OAuth
- ✅ Cloud-based storage
- ✅ Real-time updates
